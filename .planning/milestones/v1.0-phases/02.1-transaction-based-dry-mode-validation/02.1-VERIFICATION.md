---
phase: 02.1-transaction-based-dry-mode-validation
verified: 2026-04-01T16:30:00Z
status: passed
score: 8/8 must-haves verified
gaps: []
human_verification:
  - test: "Run dry-mode with a users CSV against a real OPS database and confirm zero rows persist after import"
    expected: "All processors execute, report prints, DB state is identical before and after"
    why_human: "Requires live OPS installation with database to confirm transaction rollback behavior"
  - test: "Run dry-mode with a preprints CSV containing galleys and supplementary files"
    expected: "DB processors run and roll back; no files are written to filesystem; galley/supp sections are skipped cleanly"
    why_human: "Requires live OPS installation with filesystem access to verify no file artifacts"
  - test: "Run dry-mode with multiple CSV files in source directory"
    expected: "Each file gets its own transaction; CachedEntities are fresh for each file; no cross-file state leakage"
    why_human: "Requires multi-file scenario against real database"
---

# Phase 2.1: Transaction-Based Dry-Mode Validation Verification Report

**Phase Goal:** Replace dry-mode skip-processor guards with per-file DB transaction wrapping so all processors run their real OPS code path, then DB::rollBack() undoes all writes. File system operations remain skipped. CachedEntities cleared after each rollback.
**Verified:** 2026-04-01T16:30:00Z
**Status:** passed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Dry-mode runs all processors (UsersProcessor, UserGroupsProcessor, UserInterestsProcessor) inside a DB transaction that is rolled back after each file | VERIFIED | UserCommand.php lines 81-84: beginTransaction before loop; lines 124-128: all 3 processors run unconditionally; lines 163-165: rollBack after reporting. grep confirms 0 occurrences of `dryMode.*continue` |
| 2 | Dry-mode runs all DB-writing processors (Submission, Publication, Authors, Keywords, Subjects, Funders, Sections, Categories, Statistics) inside a per-file transaction in PreprintCommand | VERIFIED | PreprintCommand.php lines 148-151: beginTransaction before loop; lines 298-473: all processors run unconditionally (old skip guard removed); lines 516-519: rollBack + reset after reporting. grep confirms 0 occurrences of `dryMode.*continue` |
| 3 | CachedEntities static arrays are cleared after each file rollback | VERIFIED | CachedEntities.php lines 62-73: reset() clears all 9 arrays. UserCommand.php line 165: CachedEntities::reset() called after rollBack. PreprintCommand.php line 518: CachedEntities::reset() called after rollBack |
| 4 | File system operations (cover image, galleys, supplementary files) remain skipped in dry-mode | VERIFIED | PreprintCommand.php line 258: `!$this->dryMode && $data->coverImageFilename` (cover image guard). Line 325: `!$this->dryMode` guard around processGalleys. Line 361: `!$this->dryMode && $data->suppFilenames` (supplementary guard) |
| 5 | CachedEntities and processedPreprints are cleared after each file rollback in PreprintCommand | VERIFIED | PreprintCommand.php line 518: CachedEntities::reset(), line 519: `$this->processedPreprints = []` |
| 6 | Post-processing (syncCoverImages, setCurrentVersions) remains skipped in dry-mode | VERIFIED | PreprintCommand.php lines 529-531: dry-mode returns early before lines 534-535 where post-processing runs |
| 7 | Normal-mode import behavior is completely unchanged | VERIFIED | All transaction calls (beginTransaction, rollBack, SET FOREIGN_KEY_CHECKS) are inside `if ($this->dryMode)` blocks. Normal-mode code path has no new transaction calls. DryModeReporter calls unchanged |
| 8 | DryModeReporter output and exit code logic remain unchanged | VERIFIED | Both commands: DryModeReporter::printFileHeader, printTableHeader, printFailedRow, printFileSummary, printGrandTotal calls are identical to pre-phase state. Exit code logic `return $totalFailed > 0 ? 1 : 0` unchanged |

**Score:** 8/8 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `classes/cachedAttributes/CachedEntities.php` | Static reset() method that clears all 9 cached arrays | VERIFIED | Lines 62-73: `public static function reset(): void` clears servers, userGroupIds, userGroups, genreIds, supplementaryGenreIds, categories, sections, users, subscriptionTypes |
| `classes/commands/UserCommand.php` | Transaction-wrapped dry-mode for user imports | VERIFIED | Contains DB import (line 29), beginTransaction (line 83), rollBack (line 163), CachedEntities::reset (line 165). Old skip guard removed. Welcome email guarded with `!$this->dryMode` (line 130) |
| `classes/commands/PreprintCommand.php` | Transaction-wrapped dry-mode for preprint imports | VERIFIED | Contains beginTransaction (line 150), rollBack (line 516), CachedEntities::reset (line 518), processedPreprints clearing (line 519). Filesystem guards on galleys (line 325), supplementary (line 361), cover image (line 258) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| UserCommand.php | Illuminate\Support\Facades\DB | DB::beginTransaction() and DB::rollBack() | WIRED | Line 29: `use Illuminate\Support\Facades\DB;` Lines 82-83: begin, Line 163-164: rollback + FK restore |
| UserCommand.php | CachedEntities.php | CachedEntities::reset() after rollback | WIRED | Line 19: import present. Line 165: reset() called after rollBack |
| PreprintCommand.php | Illuminate\Support\Facades\DB | DB::beginTransaction() and DB::rollBack() | WIRED | Line 43: `use Illuminate\Support\Facades\DB;` Lines 149-150: begin, Lines 516-517: rollback + FK restore |
| PreprintCommand.php | CachedEntities.php | CachedEntities::reset() after rollback | WIRED | Line 23: import present. Line 518: reset() called after rollBack |

### Data-Flow Trace (Level 4)

Not applicable -- this phase modifies control flow (transaction wrapping) rather than data rendering.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Test suite passes | `phpunit --configuration phpunit.xml` | SKIP -- PHPUnit binary not accessible from plugin directory (requires OPS host installation) | ? SKIP |

Step 7b: SKIPPED (PHPUnit binary requires full OPS installation at expected path)

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| TXVAL-01 | 02.1-01, 02.1-02 | Dry-mode runs all DB-writing processors inside a database transaction | SATISFIED | UserCommand lines 124-128 (3 processors), PreprintCommand lines 298-473 (all DB processors) run inside beginTransaction/rollBack |
| TXVAL-02 | 02.1-01, 02.1-02 | Each CSV file's transaction is rolled back after processing, leaving zero persistent changes | SATISFIED | UserCommand line 163: DB::rollBack(). PreprintCommand line 516: DB::rollBack(). Both per-file inside dry-mode blocks |
| TXVAL-03 | 02.1-01, 02.1-02 | CachedEntities static caches cleared after each file rollback | SATISFIED | CachedEntities::reset() exists (line 62) clearing all 9 arrays. Called in UserCommand (line 165) and PreprintCommand (line 518) after rollBack |
| TXVAL-04 | 02.1-02 | Filesystem operations remain skipped in dry-mode | SATISFIED | Cover image guard (line 258), processGalleys guard (line 325), supplementary files guard (line 361) all check `!$this->dryMode` |

No orphaned requirements found. All 4 TXVAL requirements are claimed by plans and satisfied.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | - | - | - | - |

No TODOs, FIXMEs, placeholders, or stub patterns detected in the phase-modified code. The `@TODO` at PreprintCommand line 368 is pre-existing (supplementary file exception type), not introduced by this phase.

### Human Verification Required

### 1. Live Transaction Rollback (Users)

**Test:** Run `php tools/importExport.php CSVImportExportPlugin users {username} {sourceDir} --dry-mode` against a real OPS database. Check user count before and after.
**Expected:** All user processors execute, report prints to console, database user count is identical before and after.
**Why human:** Requires live OPS installation with database to confirm rollback works end-to-end.

### 2. Live Transaction Rollback (Preprints)

**Test:** Run `php tools/importExport.php CSVImportExportPlugin preprints {username} {sourceDir} --dry-mode` with CSV containing galley and supplementary file references. Check submission count and filesystem before and after.
**Expected:** DB processors execute and roll back. No files written to filesystem. No new submissions persist.
**Why human:** Requires live OPS installation with filesystem and database access.

### 3. Multi-File Cache Isolation

**Test:** Run dry-mode with a source directory containing multiple CSV files that reference the same server/entities.
**Expected:** Each file gets fresh CachedEntities (no stale IDs from rolled-back entities). processedPreprints does not leak between files.
**Why human:** Requires multi-file scenario with entity references that would collide if cache leaked.

### Gaps Summary

No gaps found. All 8 observable truths are verified. All 4 TXVAL requirements are satisfied. All artifacts exist, are substantive, and are properly wired. The old processor-skipping guards have been completely removed from both commands. Transaction wrapping, cache clearing, and filesystem guards are correctly implemented.

**ROADMAP note:** The ROADMAP.md shows plan 01 as unchecked `[ ]` and "1/2 plans executed" despite both plans having completed summaries. This is a documentation staleness issue in ROADMAP.md, not a code gap.

---

_Verified: 2026-04-01T16:30:00Z_
_Verifier: Claude (gsd-verifier)_
